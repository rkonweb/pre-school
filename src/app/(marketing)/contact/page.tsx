import { ContactClient } from "@/components/contact/ContactClient";

export const metadata = {
    title: "Contact Us | Bodhi Board",
    description: "Get in touch with the Bodhi Board team. We are here to help you start, run, and grow your preschool with expert guidance and modern tools.",
};

export default function ContactPage() {
    return <ContactClient />;
}
